/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/teammate-management",
  assetPrefix: "/teammate-management",
  images: { unoptimized: true },
};

export default nextConfig;
